import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.cypherstudio.debttracker",
  appName: "debt-tracker",
  webDir: "dist",
};

export default config;
